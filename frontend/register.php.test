<?php

ini_set('display_errors', 1);
error_reporting(E_ALL);

echo "REGISTER PAGE LOADED<br>";

require 'config.php';

echo "CONFIG LOADED<br>";

if ($_SERVER['REQUEST_METHOD'] === 'POST') {

    echo "POST DETECTED<br>";

    $payload = [
        'name' => 'Test User2',
        'email' => 'test2@test.com',
        'password' => 'password',
        'password_confirmation' => 'password'
    ];

    $result = apiRequest(
        'POST',
        '/register',
        $payload
    );

    print_r($result);

    exit;
}
?>

<form method="POST">

    <button type="submit">
        TEST REGISTER
    </button>

</form>